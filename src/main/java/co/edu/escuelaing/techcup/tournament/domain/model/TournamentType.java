package co.edu.escuelaing.techcup.tournament.domain.model;

/**
 * Tipo de torneo: normal (con rango de fechas) o Lightning (un solo día).
 */
public enum TournamentType {
    NORMAL,
    LIGHTNING
}
